const routes = {

    // Sign up
    ABSOLUTE: "/",

    // Log in
    LOGIN: "/login",

    // Sign up
    SIGNUP: "/signup",

    // reset password if user forget it
    RESETPASSWORD: "/reset-password",

}

export default routes;